export function Loader({ keeps }) {
    // if (keeps.length === 0) return <h6>No Keeps to show <br />
    //     <span> Go ahead and create a new Keep</span></h6>

    return <div className="loader">
        <img src="./assets/img/loader.svg" alt="" />
    </div>
}

